Fixtures allow you to easily insert objects (rows) into a model (table).
Files can be JSON or YAML (and most likely other formats too).
Once created, use >python manage.py loaddata file-name to add the object.
